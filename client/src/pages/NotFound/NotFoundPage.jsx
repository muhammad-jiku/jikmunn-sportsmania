import React from 'react';
import { ErrorNotFound } from '../../components/Shared';

const NotFoundPage = () => {
  return (
    <>
      <ErrorNotFound />
    </>
  );
};

export default NotFoundPage;
