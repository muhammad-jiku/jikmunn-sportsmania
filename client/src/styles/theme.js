//  external import
import { createTheme } from '@mui/material/styles';

// exporting theme
export const theme = createTheme({
  palette: {
    primary: {
      main: '#682404',
    },
    secondary: {
      main: '#050505',
    },
  },
});
